import React, { useState, useEffect } from 'react';


export default function Services() {

    return (
        <div className="services">
            Services component
            {/* Services text -- {services.length} -- {props.parentData}
            <button type="button" onClick={()=>props.onChildMethodClick('data from services')}>Click</button> */}
        </div>
    )

}

